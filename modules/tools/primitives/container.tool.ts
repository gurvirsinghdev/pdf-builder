import { LayoutTemplateIcon } from "lucide-react";
import { Tool } from "./tool";

export const ContainerTool: Tool = {
  metadata: {
    id: "container-tool",
    name: "Container",
    icon: LayoutTemplateIcon,
  },
};
